// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to recent event routes
router.get("/", (req: Request, res: Response) => {});

// export all recent event routes
export default router;
