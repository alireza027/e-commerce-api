// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to order routes
router.get("/", (req: Request, res: Response) => {});

// export all order routes
export default router;
