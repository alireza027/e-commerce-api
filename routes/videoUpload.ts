// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to video upload routes
router.get("/", (req: Request, res: Response) => {});

// export all video upload routes
export default router;
