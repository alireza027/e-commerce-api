// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to money back method routes
router.get("/", (req: Request, res: Response) => {});

// export all money back method routes
export default router;
