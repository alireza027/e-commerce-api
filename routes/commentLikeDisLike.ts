// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to comment like dis like routes
router.get("/", (req: Request, res: Response) => {});

// export all comment like dis like routes
export default router;
