// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to post like dis like routes
router.get("/", (req: Request, res: Response) => {});

// export all post like dis like routes
export default router;
