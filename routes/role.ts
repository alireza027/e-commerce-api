// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to role routes
router.get("/", (req: Request, res: Response) => {});

// export all role routes
export default router;
