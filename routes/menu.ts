// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to menu routes
router.get("/", (req: Request, res: Response) => {});

// export all menu routes
export default router;
