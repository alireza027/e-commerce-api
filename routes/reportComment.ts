// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to report comment routes
router.get("/", (req: Request, res: Response) => {});

// export all report comment routes
export default router;
