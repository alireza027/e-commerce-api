// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to category routes
router.get("/", (req: Request, res: Response) => {});

// export all bookmark routes
export default router;
