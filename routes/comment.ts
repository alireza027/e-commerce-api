// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to comment routes
router.get("/", (req: Request, res: Response) => {});

// export all comment routes
export default router;
