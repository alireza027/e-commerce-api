// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to tag routes
router.get("/", (req: Request, res: Response) => {});

// export all tag routes
export default router;
