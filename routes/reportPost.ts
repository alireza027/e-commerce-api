// add dependencies
import { Router, Request, Response } from "express";

// use dependencies
const router = Router();

// start work to report post routes
router.get("/", (req: Request, res: Response) => {});

// export all report post routes
export default router;
